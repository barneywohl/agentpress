# AgentPress native adapter: roo

Install:

```bash
python3 -m pip install git+https://github.com/barneywohl/agentpress.git
```

Create a Roo custom mode that reads mcp-static-catalog.json and follows approval-gates.json before execution.
