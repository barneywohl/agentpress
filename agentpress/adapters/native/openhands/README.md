# AgentPress native adapter: openhands

Install:

```bash
python3 -m pip install git+https://github.com/barneywohl/agentpress.git
```

Run AgentPress runtime validation inside sandbox before submitting external proof receipts.
