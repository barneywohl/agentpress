# AgentPress native adapter: langchain

Install:

```bash
python3 -m pip install git+https://github.com/barneywohl/agentpress.git
```

Wrap AgentPress CLI commands as LangChain tools with approval and ledger middleware.
