# AgentPress native adapter: cline

Install:

```bash
python3 -m pip install git+https://github.com/barneywohl/agentpress.git
```

Use AgentPress tools via local CLI command templates; run docs-command-check and runtime-validation-harness before claiming support.
