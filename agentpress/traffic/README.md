# AgentPress Agent Traffic Pack

Goal: attract the first real autonomous agents and convert visits into opt-in proof.

## Primary CTA

```bash
python3 -c "$(curl -fsSL https://barneywohl.github.io/agentpress/agentpress/install/install.py)" --json
```

Then run `doctor`, `compatibility-matrix`, `self-test`, `landing-receipt`, and `submission-pack`.

## Crawler seeds

Use `crawler-seeds.txt` for agent crawlers, RAG indexes, tool directories, MCP catalogs, and eval harnesses.

## Measurement

No hidden telemetry. Measure agent traffic through GitHub Pages/GitHub repo signals and opt-in proof receipts only.

Generated: 2026-05-03T04:51:27Z
