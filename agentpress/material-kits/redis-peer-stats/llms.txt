# Redis: peer stats compact context

Runtime: GLM / AgentPress material kit.

Redis peer stats endpoint at https://redis.io/docs/latest/operate/rs/references/rest-api/requests/bdbs/peer_stats provides SOURCE FACT REQUIRED: the exact HTTP method, SOURCE FACT REQUIRED: the required request fields, SOURCE FACT REQUIRED: the response shape and status codes for peer statistics retrieval. Agents must use SOURCE FACT REQUIRED: the correct HTTP method and SOURCE FACT REQUIRED: the required fields to avoid silent data loss in cache/queue workflows.

Search intents:
- redis peer stats http method
- redis peer stats required fields
- redis peer stats response shape

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/redis-peer-stats --json
