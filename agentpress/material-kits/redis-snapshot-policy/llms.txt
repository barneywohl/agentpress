# Redis: snapshot policy

Runtime: GLM / AgentPress material kit.

Redis snapshot policy configuration allows SOURCE FACT REQUIRED for the specific REST API endpoint path, SOURCE FACT REQUIRED for the exact JSON payload shape, and SOURCE FACT REQUIRED for the specific HTTP status codes returned. Agents must SOURCE FACT REQUIRED the exact parameter names and SOURCE FACT REQUIRED the exact field types before attempting to apply a snapshot policy.

Search intents:
- Redis snapshot policy configuration
- Redis REST API snapshot
- Redis backup policy

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/redis-snapshot-policy --json
