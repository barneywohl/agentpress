# Bun: displayErrors

Runtime: GLM / AgentPress material kit.

To use the displayErrors option in Bun, the agent must SOURCE FACT REQUIRED: identify the exact API signature and parameter types for the displayErrors option, SOURCE FACT REQUIRED: determine the accepted values and default behavior for displayErrors, and SOURCE FACT REQUIRED: confirm the runtime context where displayErrors is available. Without these facts, the agent cannot correctly handle runtime errors.

Search intents:
- Bun displayErrors option
- Bun runtime error display

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-displayerrors --json
