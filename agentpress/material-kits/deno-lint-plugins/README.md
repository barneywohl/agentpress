# Deno: lint plugins

GLM-backed AgentPress material kit for `Deno`.

Painpoint: Agents need compact AgentPress wrapper for Deno runtime/deploy docs agents need for TS runtime work: lint plugins, but current docs are unstructured and hard to parse.

Target source: https://docs.deno.com/runtime/reference/lint_plugins/

Validation:
```bash
python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/deno-lint-plugins --json
```

DO NOT POST EXTERNALLY UNTIL HUMAN APPROVAL.
