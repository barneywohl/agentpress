# Bun: HTTP STATUS PROXY AUTHENTICATION REQUIRED

Runtime: GLM / AgentPress material kit.

To handle HTTP STATUS PROXY AUTHENTICATION REQUIRED in Bun, reference the /reference/node/http2/constants/HTTP_STATUS_PROXY_AUTHENTICATION_REQUIRED endpoint. SOURCE FACT REQUIRED: the exact HTTP status code number and description. SOURCE FACT REQUIRED: the exact response body and headers returned when this status is triggered. SOURCE FACT REQUIRED: the exact authentication requirements and proxy configuration needed to resolve this status.

Search intents:
- Bun HTTP status constants
- HTTP STATUS PROXY AUTHENTICATION REQUIRED
- Bun runtime status codes

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-http-status-proxy-auth --json
