# Bun: bytecode

GLM-backed AgentPress material kit for `Bun`.

Painpoint: Agents cannot properly configure Bun bytecode builds when setting up modern JS projects, causing build failures and runtime errors without proper bytecode configuration context.

Target source: https://bun.com/reference/bun/BuildConfig/bytecode

Validation:
```bash
python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-bytecode --json
```

DO NOT POST EXTERNALLY UNTIL HUMAN APPROVAL.
