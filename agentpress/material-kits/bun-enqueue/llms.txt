# Bun: enqueue

Runtime: GLM / AgentPress material kit.

Bun's enqueue method allows pushing data into a readable byte stream. SOURCE FACT REQUIRED: the exact method signature. SOURCE FACT REQUIRED: the exact parameter types and values accepted. SOURCE FACT REQUIRED: the exact error conditions when enqueue fails. SOURCE FACT REQUIRED: the exact return value and behavior.

Search intents:
- Bun enqueue operations context
- Bun ReadableByteStreamController enqueue

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-enqueue --json
