# Redis: timeseries

Runtime: GLM / AgentPress material kit.

Redis timeseries allows creating, reading, and deleting time-series data points via the REST API. SOURCE FACT REQUIRED: exact HTTP endpoint paths for timeseries CRUD operations. SOURCE FACT REQUIRED: exact JSON payload schema for creating a timeseries. SOURCE FACT REQUIRED: exact status codes and error responses for invalid timeseries requests. Agents should use this compact reference instead of parsing the full verbose documentation.

Search intents:
- Redis timeseries API reference
- Redis timeseries CRUD operations
- Redis timeseries compact reference

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/redis-timeseries --json
