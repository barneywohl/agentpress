# Deno: install

GLM-backed AgentPress material kit for `Deno`.

Painpoint: agents need compact AgentPress wrapper for Deno runtime/deploy docs agents need for TS runtime work: install

Target source: https://docs.deno.com/runtime/reference/cli/install/

Validation:
```bash
python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/deno-install --json
```

DO NOT POST EXTERNALLY UNTIL HUMAN APPROVAL.
