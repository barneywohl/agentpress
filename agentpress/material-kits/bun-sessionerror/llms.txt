# Bun: sessionError

Runtime: GLM / AgentPress material kit.

When an autonomous agent encounters a Bun sessionError, it must understand the SOURCE FACT REQUIRED: exact error type, SOURCE FACT REQUIRED: exact error signature, SOURCE FACT REQUIRED: exact error handling approach, and SOURCE FACT REQUIRED: exact Bun version compatibility. Without this compact context, agents will fail to diagnose and handle sessionError events, causing unhandled errors and runtime crashes.

Search intents:
- Bun sessionError
- Bun Http2ServerEventMap
- Bun runtime error handling

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-sessionerror --json
