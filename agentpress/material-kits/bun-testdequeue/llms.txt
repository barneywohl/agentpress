# Bun: TestDequeue

Runtime: GLM / AgentPress material kit.

To use TestDequeue in Bun, the agent must SOURCE FACT REQUIRED the exact method signature, SOURCE FACT REQUIRED the expected input/output types, and SOURCE FACT REQUIRED the async behavior and promise resolution. Without these, the agent cannot write valid dequeue tests and will encounter unhandled rejections.

Search intents:
- Bun TestDequeue API
- how to use TestDequeue in Bun
- Bun test dequeue async

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-testdequeue --json
