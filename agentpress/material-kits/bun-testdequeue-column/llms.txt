# Bun: column

Runtime: GLM / AgentPress material kit.

To retrieve the column property from a TestDequeue object, call the Bun TestDequeue API at SOURCE FACT REQUIRED endpoint with SOURCE FACT REQUIRED authentication. The response returns SOURCE FACT REQUIRED return shape containing SOURCE FACT REQUIRED fields for column name, SOURCE FACT REQUIRED status code, and SOURCE FACT REQUIRED callback configuration. Agents must provide SOURCE FACT REQUIRED configuration details for SOURCE FACT REQUIRED environment variables.

Search intents:
- Bun TestDequeue column
- Bun test framework column
- Bun runtime testing

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-testdequeue-column --json
