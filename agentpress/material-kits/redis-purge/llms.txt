# Redis: purge

Runtime: GLM / AgentPress material kit.

To purge a Redis database using the REST API, send a POST request to the /crdbs/purge endpoint with SOURCE FACT REQUIRED for the request body schema. The purge operation SOURCE FACT REQUIRED for required authentication headers and SOURCE FACT REQUIRED for the exact request body fields. Agents must SOURCE FACT REQUIRED for the exact response codes and SOURCE FACT REQUIRED for the exact error conditions that can occur during purge operations.

Search intents:
- redis purge api request
- redis crdbs purge
- redis rest api purge

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/redis-purge --json
