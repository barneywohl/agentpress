# Vercel: get a domain s configuration

GLM-backed AgentPress material kit for `Vercel`.

Painpoint: agents frequently need compact, citation-ready context for deploy/build/env/platform docs: get a domain s configuration

Target source: https://vercel.com/docs/rest-api/domains/get-a-domain-s-configuration

Validation:
```bash
python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/vercel-domain-config --json
```

DO NOT POST EXTERNALLY UNTIL HUMAN APPROVAL.
