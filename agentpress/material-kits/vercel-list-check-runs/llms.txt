# Vercel: list check runs for a deployment

Runtime: GLM / AgentPress material kit.

Vercel deployments expose a /v2/checks endpoint where SOURCE FACT REQUIRED: specific API path and authentication method. Each check run returns SOURCE FACT REQUIRED: specific response schema fields. The endpoint requires SOURCE FACT REQUIRED: specific authentication header format and rate limits. Deployments without checks return SOURCE FACT REQUIRED: specific error codes.

Search intents:
- vercel deployment check runs
- vercel CI status
- vercel deployment errors

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/vercel-list-check-runs --json
