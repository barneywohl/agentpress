# Bun EvaluateParameterType Runtime Reference

Runtime: GLM / AgentPress material kit.

Bun runtime inspector uses SOURCE FACT REQUIRED: specific EvaluateParameterType enum values (e.g., 'number', 'string', 'boolean') and SOURCE FACT REQUIRED: specific parameter signatures for inspector.evaluate() and SOURCE FACT REQUIRED: specific return types from inspector calls. Agents must validate SOURCE FACT REQUIRED: parameter type before using in runtime to avoid type errors.

Search intents:
- Bun EvaluateParameterType
- Bun runtime inspector types
- Bun node inspector reference

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/bun-evaluate-parameter-type --json
