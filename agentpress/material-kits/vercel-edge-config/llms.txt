# Vercel: create an edge config

Runtime: GLM / AgentPress material kit.

To create a Vercel Edge Config, the SOURCE FACT REQUIRED API endpoint is required. The SOURCE FACT REQUIRED HTTP method and request body schema are needed. The SOURCE FACT REQUIRED authentication scope and project ID format are required. The SOURCE FACT REQUIRED response shape and status codes for success and failure are needed. The SOURCE FACT REQUIRED environment variable or config parameter for region selection is required.

Search intents:
- Vercel Edge Config create API
- Vercel deploy config setup

Validation command: python3 scripts/agentpress.py material-kit-validate agentpress/material-kits/vercel-edge-config --json
